{{ form_start(form, {attr:{'class' : 'mantenimiento', 'id':'equipoform'}}) }}
    <div class="ocultate">
    {{ form_row(form.usuarioEmail) }}
</div>
    {{form_widget(form)}}
    <button class="btn btn-primary">{{ button_label|default('Guardar') }}</button>
     <a class="btn btn-primary" href="{{ path('build_index') }}">Volver al listado</a>
{{ form_end(form) }}

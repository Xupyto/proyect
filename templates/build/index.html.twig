{% extends 'base.html.twig' %}

{% block title %}Listado de equipos{% endblock %}

{% block body %}
    <h1  class="titulo">Listado de equipos</h1>

    <table class="table table-hover table-dark titulo">
        <thead>
            <tr>
                
                <th>Nombre</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for equipo in equipos %}
            <tr>
                
                <td>{{ equipo.nombre }}</td>
                <td>
                    <a class="btn btn-primary" href="{{ path('equipo_show', {'idequipo': equipo.idequipo}) }}">Mostrar</a>
                    <a class="btn btn-primary" href="{{ path('build_edit', {'idequipo': equipo.idequipo}) }}">Editar</a>
                    {{ include('build/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">No hay resultados.</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    {# display navigation #}
<div class="navigation">
    {{ knp_pagination_render(equipos) }}
</div>

    <a class="btn btn-primary crear" href="{{ path('build_new') }}">Crear nuevo</a>
{% endblock %}

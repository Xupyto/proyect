{% extends 'base.html.twig' %}

{% block title %}Equipo{% endblock %}

{% block body %}
    <h1 class="titulo">Equipo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id equipo</th>
                <td>{{ equipo.idequipo }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ equipo.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('build_index') }}">Volver al listado</a>

    <a class="btn btn-primary" href="{{ path('build_edit', {'idequipo': equipo.idequipo}) }}">Editar</a>

    {{ include('build/_delete_form.html.twig') }}
{% endblock %}

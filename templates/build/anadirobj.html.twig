{% extends 'base.html.twig' %}

{% block body %}
<form action="{{path('add_obj', {'idpoke': poke.idpoke, 'idequipo': equipo.idequipo })}}" method="POST" class="mantenimiento">
    
        <select name="obj" required>
          {% for obj in objetos %}
            <option value ="{{obj.objetoIdobjeto.idobjeto}}">{{obj.objetoIdobjeto.nombre}}</option>
          {% else %}
            <option value ="">No hay objetos.</option>
        {% endfor %}
        </select>
    
     <input type="submit" value="Confirmar" class="btn btn-primary" />
</form>

{% endblock %}

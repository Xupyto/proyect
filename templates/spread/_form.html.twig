{{ form_start(form, {attr:{'class' : 'mantenimiento', 'id':'spreadform'}}) }}

    {{ form_widget(form) }}
    <button class="btn btn-primary">{{ button_label|default('Guardar') }}</button>
     <a class="btn btn-primary" href="{{ path('spread_index') }}">Volver al listado</a>
{{ form_end(form) }}

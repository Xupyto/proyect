{% extends 'base.html.twig' %}

{% block title %}PokePuedeAprenderMov{% endblock %}

{% block body %}
    <h1>PokePuedeAprenderMov</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>PorcentajeUso</th>
                <td>{{ poke_puede_aprender_mov.porcentajeUso }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('poke_puede_aprender_mov_index') }}">back to list</a>

    <a href="{{ path('poke_puede_aprender_mov_edit', {'formato': poke_puede_aprender_mov.formato}) }}">edit</a>

    {{ include('poke_puede_aprender_mov/_delete_form.html.twig') }}
{% endblock %}

{{ form_start(form, {attr:{'class' : 'mantenimiento', 'id':'campeonatoform'}}) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">{{ button_label|default('Guardar') }}</button>
     <a class="btn btn-primary" href="{{ path('campeonato_index') }}">Volver al listado</a>
{{ form_end(form) }}

<form method="post" action="{{ path('pokemon_delete', {'idpoke': poke.idpoke}) }}" onsubmit="return confirm('¿Seguro que quieres borrar este pokemon?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ poke.idpoke) }}">
    <button class="btn btn-primary">Borrar</button>
</form>

<form method="post" action="{{ path('formato_delete', {'id': formato.id}) }}" onsubmit="return confirm('¿Seguro que quieres borrarlo?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formato.id) }}">
    <button class="btn">Delete</button>
</form>
